export class Items {

    title : string;
    priority : string;
    description : string;

    constructor(title:string,priority:string,desc:string)
    {
        this.title = title;
        this.priority = priority;
        this.description = desc;
    }

}
